import 'package:flutter/material.dart';

class GlorioColors {
  GlorioColors._();

  // Calm neutral palette (warm off-white background, sand/greige surfaces, dusty sage/blue accents)
  static const Color background = Color(0xFFF7F4EE); // warm off-white
  static const Color textPrimary = Color(0xFF33302C); // soft near-black (warm)
  static const Color textMuted = Color(0xFF7B7874); // muted greige
  static const Color card = Color(0xFFF1ECE6); // light sand/greige for cards (not pure white)
  static const Color cardAlt = Color(0xFFEDE8E2); // subtle alternate surface
  static const Color border = Color(0xFFE7E2DB); // very soft border tone
  static const Color accent = Color(0xFF8AA19A); // dusty sage green as rare accent
  static const Color pale = Color(0xFFF3F1EE); // gentle pale surface
  static const Color warning = Color(0xFFB66A53); // muted terracotta for warnings/errors
}
